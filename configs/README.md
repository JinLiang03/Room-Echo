# configs

Runtime configuration lives here or in the environment (see `.env.example`).

Planned contents:

- calibration profiles (empty-room baseline parameters, topology hashes)
- replay scenario descriptors
- session presets

No API credentials or serial device guesses belong in committed files.
